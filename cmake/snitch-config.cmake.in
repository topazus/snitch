@PACKAGE_INIT@

if(NOT TARGET snitch::snitch)
    include(${CMAKE_CURRENT_LIST_DIR}/snitch-targets.cmake)
endif()
if(NOT TARGET snitch::snitch-header-only)
    include(${CMAKE_CURRENT_LIST_DIR}/snitch-header-only-targets.cmake)
endif()